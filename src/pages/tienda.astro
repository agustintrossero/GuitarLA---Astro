---
import Layout from '../layouts/Layout.astro';
import Guitarra from '../components/Guitarra.astro'

const url = `${import.meta.env.PUBLIC_API_URL}/api/guitarras?populate=imagen`
const respuesta = await fetch(url)
const {data} = await respuesta.json()
//console.log(data)
---

<Layout title="Tienda" description="Pagina de Venta de Guitarras">
	<main class="contenedor">
		<div class="heading">Nuestra Colección</div>
		<div class="grid">
			{data?.map(guitarra =>(
				<Guitarra
					nombre = {guitarra.attributes.nombre}
					descripcion = {guitarra.attributes.descripcion}
					url = {guitarra.attributes.url}
					precio = {guitarra.attributes.precio}
					imagen = {guitarra.attributes.imagen.data.attributes.formats.medium.url}
				/>
			))}
		</div>
	</main>
</Layout>

<style>
	
</style>